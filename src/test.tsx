import React from 'react';
import { css } from "@linaria/core";

export const Test = () => {
    return (
        <div className={style}>
            test
        </div>
    );
};

const style = css`
    color: red
`;